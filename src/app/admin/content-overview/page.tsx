import type { Metadata } from 'next';
export { default } from './ContentOverviewClient';

export const metadata: Metadata = {
  title: 'Admin — Content Overview',
  robots: { index: false, follow: false },
};
