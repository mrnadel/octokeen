import type { Metadata } from 'next';
export { default } from './SubscriptionsClient';

export const metadata: Metadata = {
  title: 'Admin — Subscriptions',
  robots: { index: false, follow: false },
};
