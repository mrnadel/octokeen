import type { Metadata } from 'next';
export { default } from './GameBalanceClient';

export const metadata: Metadata = {
  title: 'Admin — Game Balance',
  robots: { index: false, follow: false },
};
